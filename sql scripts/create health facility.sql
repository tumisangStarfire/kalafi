CREATE TABLE `Graphite_dev`.`HealthFacility` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `latitude` VARCHAR(45) NULL DEFAULT NULL,
    `longitude` VARCHAR(45) NULL DEFAULT NULL,
    `name` VARCHAR(45) NULL DEFAULT NULL,
    PRIMARY KEY (`id`)
);